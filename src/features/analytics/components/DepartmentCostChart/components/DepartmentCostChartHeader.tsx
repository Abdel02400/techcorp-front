import { CardDescription, CardHeader, CardTitle } from '@/shared/components/ui/card';

export const DepartmentCostChartHeader = () => (
    <CardHeader>
        <CardTitle>Department Cost Breakdown</CardTitle>
        <CardDescription>Active tools only, grouped by owning department.</CardDescription>
    </CardHeader>
);
