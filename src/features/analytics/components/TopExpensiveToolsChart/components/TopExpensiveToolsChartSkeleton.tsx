import { CardContent } from '@/shared/components/ui/card';
import { Skeleton } from '@/shared/components/ui/skeleton';

export const TopExpensiveToolsChartSkeleton = () => (
    <CardContent>
        <Skeleton className="h-80 w-full" />
    </CardContent>
);
