import { CardDescription, CardHeader, CardTitle } from '@/shared/components/ui/card';

export const StatusDistributionChartHeader = () => (
    <CardHeader>
        <CardTitle>Tool Status Distribution</CardTitle>
        <CardDescription>Active, expiring, and unused tools across the catalogue.</CardDescription>
    </CardHeader>
);
